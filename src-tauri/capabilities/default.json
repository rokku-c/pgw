{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell talks to the gateway over HTTP from Rust, so the webview needs no IPC permissions of its own.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
